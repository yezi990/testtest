#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_microtemplate@1.0.2@microtemplate/bin/microtemplate" "$@"
  ret=$?
else 
  node  "$basedir/../../../_microtemplate@1.0.2@microtemplate/bin/microtemplate" "$@"
  ret=$?
fi
exit $ret
